# Copy to .env and fill in. .env is gitignored — never commit real secrets.
POSTGRES_USER=cctracker
POSTGRES_PASSWORD=cctracker
POSTGRES_DB=cctracker

# Cloudflare Tunnel token for a remotely-managed tunnel (Zero Trust dashboard).
# Map the public hostname to http://app:8080.
CLOUDFLARE_TUNNEL_TOKEN=
